Django>=5.2
python-dotenv>=1.0
requests>=2.32

# Local HTTPS for runserver_plus — CyberSource's capture-context API
# rejects http:// origins, so testing checkout locally needs TLS.
django-extensions>=3.2
Werkzeug>=3.0
cryptography>=42.0
pyOpenSSL>=24.0
